# Core dependencies for AI_clipper
# Pin versions later after validation (`pip freeze > requirements.lock.txt`)

openai-whisper
openai
moviepy
yt_dlp
os
# torch  # Uncomment and specify version if GPU acceleration needed (e.g. torch==2.3.0+cu121)
